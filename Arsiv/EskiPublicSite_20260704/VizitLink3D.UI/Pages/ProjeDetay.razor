@page "/projeler/{Slug}"
@layout VizitLink3D.UI.Layout.VizitLink3DDuzen

<PageTitle>@_sayfaBasligi</PageTitle>

@if (_yukleniyor)
{
    <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="py-16 d-flex justify-center align-center gb-yukleniyor-alani">
        <MudProgressCircular Color="Color.Primary" Size="Size.Large" Indeterminate="true" />
    </MudContainer>
}
else if (_proje is null)
{
    <MudContainer MaxWidth="MaxWidth.Large" Class="py-16 text-center gb-bos-liste-alani">
        <MudIcon Icon="@Icons.Material.Filled.SearchOff" Size="Size.Large" Class="gb-bos-liste-ikon" />
        <MudText Typo="Typo.h5">@dil.T("projeDetay.bulunamadi", "Proje bulunamadi")</MudText>
        <MudButton Href="/projeler" Variant="Variant.Filled" Color="Color.Primary" StartIcon="@Icons.Material.Filled.ArrowBack" Class="mt-4">
            @dil.T("projeDetay.projelereDon", "Projelere don")
        </MudButton>
    </MudContainer>
}
else
{
    <section class="gb-detay-hero">
        <img src="@ProjeGorseli(_proje)" alt="@_proje.Baslik" class="gb-detay-hero-gorsel" />
        <div class="gb-detay-hero-katman">
            <MudContainer MaxWidth="MaxWidth.ExtraLarge">
                <MudStack Spacing="4" Class="gb-detay-hero-icerik">
                    <MudButton Href="/projeler" Variant="Variant.Text" Color="Color.Primary" StartIcon="@Icons.Material.Filled.ArrowBack" Class="gb-detay-geri-btn">
                        @dil.T("projeDetay.geri", "Projeler")
                    </MudButton>
                    <MudText Typo="Typo.overline" Class="gb-detay-ust-etiket">@dil.T("projeDetay.ustEtiket", "Endustriyel uygulama dosyasi")</MudText>
                    <MudText Typo="Typo.h2" Class="gb-detay-baslik">@_proje.Baslik</MudText>
                    @if (!string.IsNullOrWhiteSpace(_proje.KisaAciklama))
                    {
                        <MudText Typo="Typo.subtitle1" Class="gb-detay-ozet">@_proje.KisaAciklama</MudText>
                    }
                    <div class="gb-detay-metrikler">
                        <div>
                            <span>@dil.T("projeDetay.konum", "Konum")</span>
                            <strong>@DegerYaz(_proje.MusteriSehir)</strong>
                        </div>
                        <div>
                            <span>@dil.T("projeDetay.tarih", "Tarih")</span>
                            <strong>@ProjeTarihiYaz(_proje)</strong>
                        </div>
                        <div>
                            <span>@dil.T("projeDetay.durum", "Durum")</span>
                            <strong>@dil.T("projeDetay.tamamlandi", "Tamamlandi")</strong>
                        </div>
                    </div>
                </MudStack>
            </MudContainer>
        </div>
    </section>

    <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="py-12">
        <MudGrid Spacing="6">
            <MudItem xs="12" lg="8">
                <MudPaper Elevation="0" Class="vizit-glass-card vizit-border-glow">
                    <MudText Typo="Typo.overline" Class="vizit-glass-card vizit-border-glow-etiket">@dil.T("projeDetay.projeNotu", "Proje notu")</MudText>
                    <MudText Typo="Typo.h4" Class="vizit-glass-card vizit-border-glow-baslik">@dil.T("projeDetay.uygulamaDetayi", "Uygulama detayi")</MudText>
                    <MudText Typo="Typo.body1" Class="gb-detay-metin">@AciklamaYaz(_proje)</MudText>
                </MudPaper>

                <MudGrid Spacing="3" Class="mt-6">
                    @foreach (var madde in SurecMaddeleri)
                    {
                        <MudItem xs="12" md="4">
                            <MudPaper Elevation="0" Class="vizit-glass-card vizit-border-glow">
                                <MudIcon Icon="@madde.Ikon" />
                                <MudText Typo="Typo.subtitle2">@madde.Baslik</MudText>
                                <MudText Typo="Typo.body2">@madde.Aciklama</MudText>
                            </MudPaper>
                        </MudItem>
                    }
                </MudGrid>
            </MudItem>

            <MudItem xs="12" lg="4">
                <MudPaper Elevation="0" Class="vizit-glass-card vizit-border-glow">
                    <MudText Typo="Typo.h6">@dil.T("projeDetay.enCokIncelenen", "En cok incelenen projeler")</MudText>
                    <MudStack Spacing="2" Class="mt-4">
                        @foreach (var proje in _enCokIncelenenler)
                        {
                            <MudLink Href="@ProjeDetayBaglantisi(proje)" Underline="Underline.None" Class="gb-yan-liste-ogesi">
                                <img src="@ProjeGorseli(proje)" alt="@proje.Baslik" />
                                <span>@proje.Baslik</span>
                            </MudLink>
                        }
                    </MudStack>
                </MudPaper>
            </MudItem>
        </MudGrid>
    </MudContainer>
}

