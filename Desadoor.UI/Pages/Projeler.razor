@page "/projeler"
@layout Desadoor.UI.Layout.DesaDoorDuzen

<PageTitle>@dil.T("projeler.baslik", "Projelerimiz") — DesaDoor</PageTitle>

<Desadoor.UI.Bilesenler.HeroSlider SayfaKodu="projeler" />

@if (_yukleniyor)
{
    <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="py-16 d-flex justify-center align-center desa-yukleniyor-alani">
        <MudProgressCircular Color="Color.Primary" Size="Size.Large" Indeterminate="true" />
    </MudContainer>
}
else if (!_projeler.Any())
{
    <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="py-16 text-center desa-bos-liste-alani">
        <MudIcon Icon="@Icons.Material.Filled.Construction" Size="Size.Large" Class="desa-bos-liste-ikon" />
        <MudText Typo="Typo.h6" Class="mt-4">@dil.T("projeler.bos", "Henuz proje eklenmemis.")</MudText>
    </MudContainer>
}
else
{
    <MudPaper Elevation="0" Class="desa-hero-section desa-liste-hero d-flex align-center justify-center pa-4">
        <MudStack AlignItems="AlignItems.Center" Spacing="4" Class="text-center">
            <MudText Typo="Typo.h2" Class="desa-hero-baslik">@_heroBaslik</MudText>
            <MudText Typo="Typo.subtitle1" Class="desa-hero-altbaslik desa-hero-aciklama">
                @_heroAciklama
            </MudText>
        </MudStack>
    </MudPaper>

    <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="py-16">
        <MudGrid Spacing="4">
            @foreach (var proje in _projeler)
            {
                <MudItem Class="anim-scroll-yukari" xs="12" sm="6" md="4" lg="3">
                    <MudCard Elevation="0" Class="desa-proje-kart">
                        <MudLink Href="@ProjeDetayBaglantisi(proje)" Underline="Underline.None" Class="desa-proje-kart-baglanti">
                            <div class="desa-gorsel-kutu">
                                <img src="@ProjeGorseli(proje)" alt="@proje.Baslik" />
                                @if (!string.IsNullOrEmpty(proje.MusteriLogo))
                                {
                                    <div class="desa-proje-logo-kutu">
                                        <img src="@ProjeLogo(proje)" alt="@proje.MusteriAdi" class="desa-proje-logo" />
                                    </div>
                                }
                                <div class="desa-hover-overlay">
                                    <MudText Typo="Typo.h6" Class="desa-proje-kart-baslik">@proje.Baslik</MudText>
                                    @if (!string.IsNullOrEmpty(proje.KisaAciklama))
                                    {
                                        <MudText Typo="Typo.body2" Class="desa-proje-kart-ozet">@proje.KisaAciklama</MudText>
                                    }
                                    @if (!string.IsNullOrEmpty(proje.MusteriSehir))
                                    {
                                        <MudChip T="string" Size="Size.Small" Class="desa-proje-kart-konum">@proje.MusteriSehir</MudChip>
                                    }
                                    <span class="desa-kart-detay-btn">
                                        @dil.T("projeler.detayiIncele", "Detayi incele")
                                        <MudIcon Icon="@Icons.Material.Filled.ArrowRightAlt" Size="Size.Small" />
                                    </span>
                                </div>
                            </div>
                        </MudLink>
                    </MudCard>
                </MudItem>
            }
        </MudGrid>
    </MudContainer>
}
