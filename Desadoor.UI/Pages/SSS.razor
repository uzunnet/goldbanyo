@page "/sss"
@layout DesaDoorDuzen

<PageTitle>@dil.T("sss.baslik", "Sıkça Sorulan Sorular") — DesaDoor</PageTitle>

<Desadoor.UI.Bilesenler.HeroSlider SayfaKodu="sss" />

@if (_yukleniyor)
{
    <MudContainer MaxWidth="MaxWidth.Large" Class="py-16 d-flex justify-center align-center" Style="min-height: 60vh;">
        <MudProgressCircular Color="Color.Primary" Size="Size.Large" Indeterminate="true" />
    </MudContainer>
}
else
{
    <MudPaper Elevation="0" Square="true" Style="min-height: 35vh; background-color: var(--desa-secondary); color: var(--desa-bg-base); display: flex; align-items: center; justify-content: center; padding: 64px 0;">
        <MudStack AlignItems="AlignItems.Center" Spacing="4" Class="text-center">
            <MudText Typo="Typo.h2" Style="color: var(--desa-bg-base); font-family: 'Noto Serif', serif;">@_heroBaslik</MudText>
        </MudStack>
    </MudPaper>

    <MudContainer MaxWidth="MaxWidth.Large" Class="py-16">
        @if (_sorular.Any())
        {
            <MudExpansionPanels Elevation="0" Class="desa-sss-panel">
                @foreach (var soru in _sorular)
                {
                    <MudExpansionPanel Text="@soru.Soru" Style="border-bottom: 1px solid var(--desa-border); margin-bottom: 8px;">
                        <MudText Typo="Typo.body1" Style="color: var(--desa-secondary); line-height: 1.8; padding: 8px 0;">@soru.Cevap</MudText>
                    </MudExpansionPanel>
                }
            </MudExpansionPanels>
        }
        else
        {
            <MudText Typo="Typo.body1" Class="text-center" Style="color: var(--desa-secondary);">@dil.T("sss.icerikYok", "Henuz SSS icerigi eklenmemistir.")</MudText>
        }
    </MudContainer>
}
